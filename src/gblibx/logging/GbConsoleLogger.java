package gblibx.logging;

import java.util.logging.StreamHandler;

/**
 * Console logger which sends non-error messages to stdout and errors to stderr.
 */
public class GbConsoleLogger extends StreamHandler {
}
